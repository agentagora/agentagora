{
  "name": "@agentagora/status",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AgentAgora public status page — pull-based uptime indicator that pings cloud-api /healthz on each request.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.9.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  }
}
